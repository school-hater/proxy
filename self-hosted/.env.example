# PondPilot CORS Proxy - Environment Configuration
# Copy this file to .env and customize for your deployment

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS - Origins that can use this proxy
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://app.pondpilot.io,http://localhost:5173

# Security - Domain Allowlist (RECOMMENDED)
# Only allow proxying to these domains (comma-separated patterns)
# Use * for wildcards: *.s3.amazonaws.com
# Leave empty to use secure defaults (see shared/security.ts)
#
# Default allowlist includes:
# - AWS S3 (*.s3.amazonaws.com, *.s3.*.amazonaws.com)
# - CloudFront (*.cloudfront.net)
# - GitHub (*.github.io, *.githubusercontent.com)
# - Google Cloud Storage (*.storage.googleapis.com)
# - Azure Blob Storage (*.blob.core.windows.net)
# - Common CDNs and data repositories
#
# Examples:
# ALLOWED_DOMAINS=*.s3.amazonaws.com,*.cloudfront.net,data.example.com
# ALLOWED_DOMAINS=your-bucket.s3.amazonaws.com  # Only specific bucket
#
ALLOWED_DOMAINS=

# Security - HTTPS Enforcement
# Set to true to only allow HTTPS URLs (recommended for production)
# In production mode (NODE_ENV=production), this is automatically true
HTTPS_ONLY=true

# Rate Limiting
# Maximum requests per IP address per time window
RATE_LIMIT_REQUESTS=60
# Time window in milliseconds (60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# File Size Limits
# Maximum file size to proxy in MB
MAX_FILE_SIZE_MB=500

# Request Timeout
# Maximum time to wait for remote server response in milliseconds
REQUEST_TIMEOUT_MS=30000

# Bug Reporting Integration (Optional)
# Slack webhook URL for receiving bug reports from PondPilot
# If configured, the proxy will forward bug reports to your Slack channel
# Create a webhook at: https://api.slack.com/messaging/webhooks
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Logging (development only)
# Set NODE_ENV=development to enable request logging
# In production, NO request data is logged for privacy
